anguage: c                                                                     
compiler:
  - gcc
  - clang
# Change this to your needs 
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq libdbus-1-dev
script: make
